
test('测试fun1', () => {
  // 引入文件
  let src = require('../class.js');
  // 调用方法
  let res = src.fun1();
  
  // 断言
  //expect(res)
  //  .toBe('is compare result');
});
      

test('测试fun2', () => {
  // 引入文件
  let src = require('../class.js');
  // 调用方法
  let res = src.fun2();
  
  // 断言
  //expect(res)
  //  .toBe('is compare result');
});
      